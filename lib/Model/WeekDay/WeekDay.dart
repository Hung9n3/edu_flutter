class Weekday {
  int id;
  String Day;
  Weekday(this.id, this.Day);
}
List<Weekday> days = [Weekday(1, 'Monday'), Weekday(2, 'Tuesday'), Weekday(3, 'Wednesday'), Weekday(4, 'Thursday'), Weekday(5, 'Friday'), Weekday(6, 'Saturday')];